const express = require('express');
const router = express.Router();

router.get('/', function(req, res) {
  // code to retrieve events
});

router.post('/', function(req, res) {
  // code to create new events
});

module.exports = router;
